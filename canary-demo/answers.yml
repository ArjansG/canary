# Deployment Information
pods_status:
  main_pods_running: 3
  canary_pods_running: 1

# Service Information
service_endpoints:
  main_service_cluster_ip: 10.101.229.66 
  canary_service_cluster_ip: 10.111.14.28

# Ingress Information
ingress_details:
  address: 192.168.49.2
  host: canary-demo.local

# Metrics
main_deployment_metrics:
  http_requests_total: http_requests_total{version="v1"} 11.0
  process_cpu_seconds_total: process_cpu_seconds_total 0.5800000000000001
  process_resident_memory_bytes: process_resident_memory_bytes 3.2456704e+07

canary_deployment_metrics:
  http_requests_total: http_requests_total{version="v2"} 8.0
  process_cpu_seconds_total: process_cpu_seconds_total 0.61
  process_resident_memory_bytes: process_resident_memory_bytes 3.2571392e+07

# Traffic Distribution Test
traffic_test_results:
  total_requests_sent: 0, something is wrong here
  main_responses_received: #TODO: Enter how many responses were from main deployment
  canary_responses_received: #TODO: Enter how many responses were from canary deployment
  actual_canary_percentage: #TODO: Calculate the actual percentage of canary traffic

# Prometheus Queries
prometheus_metrics:
  main_request_rate: #TODO: Enter the result of rate(http_requests_total{version="v1"}[5m])
  canary_request_rate: #TODO: Enter the result of rate(http_requests_total{version="v2"}[5m])

# Rollback Test
rollback_test:
  previous_revision: 2
  rollback_command_used: helm rollback canary-demo 1 -n canary-demo
  time_to_rollback_seconds: didnt mention, but instant

# Error Budget Calculation (based on 99.9% SLO)
error_budget:
  monthly_error_budget_seconds: 2592.00
  remaining_error_budget_percentage: 100%

# Additional Observations
observations:
  unexpected_behaviors: #TODO: List any unexpected behaviors you observed
  suggested_improvements: #TODO: List any improvements you would suggest
